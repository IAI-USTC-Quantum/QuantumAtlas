# QuantumAtlas Web

Vite + React 19 + TypeScript workspace for the QuantumAtlas web shell.

## Stack

- **Vite 7** — build / dev server
- **React 19**
- **TypeScript 5.6** with `@/*` path alias (`./src/*` — note: also in the root
  `tsconfig.json` so the shadcn CLI can resolve it)
- **Tailwind CSS v4** via `@tailwindcss/vite` (CSS-first config in `src/index.css`
  using `@theme inline` + shadcn light/dark CSS variables; `App.css` is gone)
- **shadcn/ui** (`components.json`, 17 components installed under
  `components/ui/`: alert, badge, button, card, dialog, dropdown-menu, input,
  label, scroll-area, select, separator, sheet, skeleton, sonner, tabs,
  textarea, tooltip)
- **TanStack Router** (file-based routes under `src/routes/`)
- **TanStack Query** (data fetching hooks in `src/lib/queries.ts`)
- **react-i18next + i18next-browser-languagedetector** — URL-prefix
  internationalisation (`/zh/*`, `/en/*`, default `zh`)
- **next-themes** — light/dark/system theme (storage key `qatlas_theme`)
- **lucide-react** for icons
- **marked + DOMPurify** for wiki markdown rendering
- **sonner** for toasts (mounted in `main.tsx`)

## Layout

```
web/
├── components.json                 # shadcn/ui config
├── vite.config.ts                  # tailwind + tanstack-router + react plugins, @ alias
├── src/
│   ├── main.tsx                    # Theme/Tooltip/Toaster providers + Router/Query
│   ├── index.css                   # @import "tailwindcss" + @theme inline tokens
│   ├── i18n/
│   │   ├── index.ts                # i18next bootstrap (8 namespaces, path detection)
│   │   └── locales/{zh,en}.json    # canonical strings (zh is default)
│   ├── hooks/use-lang.ts           # safe wrapper for the :lang route param
│   ├── routeTree.gen.ts            # auto-generated by TanStack Router (committed)
│   ├── lib/
│   │   ├── api.ts                  # types + getJson + selectors
│   │   ├── queries.ts              # useQuery hooks (Bearer from pb.authStore)
│   │   ├── pb.ts                   # PocketBase JS SDK singleton
│   │   ├── auth.ts                 # OAuth login / completeOAuth2Login / useAuth
│   │   └── utils.ts                # cn() and small helpers
│   ├── components/
│   │   ├── ui/                     # shadcn primitives (kebab-case)
│   │   ├── sidebar.tsx topbar.tsx
│   │   ├── language-switcher.tsx theme-toggle.tsx theme-provider.tsx
│   │   ├── panel.tsx page-header.tsx page-card.tsx page-list-item.tsx
│   │   └── metric-grid.tsx status-block.tsx
│   └── routes/
│       ├── __root.tsx              # auth gate (no chrome)
│       ├── index.tsx               # redirects to /{detectedLang}
│       ├── login.tsx auth.callback.tsx
│       ├── $lang.tsx               # layout: sidebar + topbar + outlet, syncs i18n
│       ├── $lang.index.tsx         # home
│       ├── $lang.wiki.index.tsx wiki.search.tsx wiki.page.$.tsx
│       ├── $lang.graph.index.tsx graph.node.$.tsx
│       └── $lang.token.tsx $lang.pat.tsx
```

## Routes

Every user-facing route lives under a `/{lang}/` prefix (`zh` default, `en`
opt-in). The bare `/` redirects to `/{detectedLang}` based on (1) the
`qatlas_lang` localStorage key, (2) the browser `Accept-Language` header,
(3) `DEFAULT_LANG=zh`. Auth-only routes (`/login`, `/auth/callback`) stay
unprefixed.

| URL pattern | Route file |
|---|---|
| `/` | `routes/index.tsx` (redirect only) |
| `/login`, `/auth/callback` | `routes/login.tsx`, `routes/auth.callback.tsx` |
| `/{lang}` | `routes/$lang.index.tsx` (home) |
| `/{lang}/wiki` | `routes/$lang.wiki.index.tsx` |
| `/{lang}/wiki/search?q=...` | `routes/$lang.wiki.search.tsx` |
| `/{lang}/wiki/page/<id>` (id may contain `/`) | `routes/$lang.wiki.page.$.tsx` |
| `/{lang}/graph` | `routes/$lang.graph.index.tsx` |
| `/{lang}/graph/node/<type>/<id>` | `routes/$lang.graph.node.$.tsx` |
| `/{lang}/pat` | `routes/$lang.pat.tsx` |

These all serve the same SPA shell mounted by the Go server's
`apis.Static` handler.

## Dev workflow

`npm run dev` only serves the SPA bundle. There is no built-in API
backend, so `fetch('/api/...')` would fall back to `index.html` and the
SPA would crash with `Unexpected token '<', "<!doctype "... is not
valid JSON`. To work on the UI you have two options:

1. **Run a local qatlasd** alongside vite (recommended when you
   are also changing Go handlers). Build and run:
   ```bash
   pixi run -- go run ./cmd/qatlasd serve --http=127.0.0.1:4200
   ```
2. **Reverse-proxy to a remote qatlasd** (recommended when you
   only touch frontend and want real data). Any deployment you can
   reach over HTTP(S) works — production, staging, a self-hosted dev
   instance, etc.

Either way, point vite at it:

```bash
cp web/.env.development.example web/.env.development.local
# edit web/.env.development.local — at minimum set VITE_DEV_API_TARGET
```

Available knobs (see `web/.env.development.example` for the
authoritative descriptions):

| Variable | What it does |
|---|---|
| `VITE_DEV_API_TARGET` | URL vite reverse-proxies `/api`, `/_`, `/share` and `/swagger` to. **Required** for any UI that hits the server. Self-signed certs are accepted. |
| `VITE_DEV_FAKE_AUTH` | When `1`, the SPA's PocketBase login gate returns a stub authed state so you go straight into protected views without OAuth. Read endpoints work through the proxy; write endpoints still 401. Stripped from production builds via `import.meta.env.DEV` dead-code elimination. |
| `VITE_DEV_ALLOWED_HOSTS` | Comma-separated extra `Host` headers vite accepts. Needed when a reverse proxy fronts the dev server on a non-loopback hostname/IP — vite 5+ rejects unknown Hosts by default. |

WebSocket / HMR is automatic — vite's own ws endpoints are not under
`/api`, so they bypass the proxy.

## Internationalisation

The URL prefix is the **authoritative** signal — the `$lang` layout route
reads `:lang` from the route params and calls `i18n.changeLanguage()` on
every match. Localised language switching is just navigation: the
`LanguageSwitcher` component computes the equivalent path under the
target lang and navigates there with `router.navigate({ to, search })`,
preserving existing query string. No re-render games, no flash of stale
content.

- Bootstrap: `src/i18n/index.ts`
- Namespaces: `common home wiki graph token pat login auth`
- Strings: `src/i18n/locales/{zh,en}.json`
- React hook: `useTranslation('namespace')` or `<Trans i18nKey="..."
  components={{ ... }} />` for inline links
- Add a string → edit both JSON files, no codegen step

## Theming

`next-themes` provides light / dark / system, persisted under
`qatlas_theme`. Colour tokens live in `src/index.css` under `:root` and
`.dark`, exposed to Tailwind via `@theme inline`. shadcn primitives use
those tokens directly (`bg-background`, `text-foreground`,
`border-border`, etc.).

## Common commands

```bash
npm install
npm run lint
npm run build              # tsr generate && tsc -b && vite build
npx shadcn@latest add <c>  # add another shadcn primitive
```

The build output (`dist/`) is embedded into the Go binary via
`internal/webui/spa.go` `//go:embed`; rebuild the Go server after a web
change is needed in production.

## Notes

- `routeTree.gen.ts` is committed by convention so `tsc` can run without
  first invoking `tsr generate` in editors / CI.
- All component file names are kebab-case to match shadcn convention.
- `@/*` alias must be configured in **both** `tsconfig.json` and
  `tsconfig.app.json` for the shadcn 4.x CLI to resolve target paths.

