# Vite 本地开发配置：复制为 web/.env.development.local（*.local 已忽略）。
# 默认只连接隔离的本机 Go 后端，不填写生产目标或生产凭据。
# 源码 dev/pseudo 版本 serve 仍需先构建 Sphinx 两站 + npm，再带 -tags embedui；
# 正确的 --config / 临时 HOME / XDG 隔离命令见 web/README.md。
#
# 注意：VITE_ 是 Vite 可暴露给客户端的变量前缀，不是秘密存储机制。
# “开发代理使用”不保证永不进入产物；生产打包前确保 shell 与所有会加载的
# .env 文件不含 token，不使用携带凭据的 development mode 打包。

# 当前 vite.config.ts 代理 /api、/_（PocketBase）、/share、/swagger。
# 不代理 /doc 或 /devdoc；请在后端 URL 验证文档及管理员门控。
# 未设置目标时 API 请求可能返回 SPA HTML，出现 Unexpected token '<'。
# 所有代理项 secure=false，会接受未验证的上游 HTTPS 证书；不是生产安全默认。
VITE_DEV_API_TARGET=http://127.0.0.1:4200

# 设为 1 只让前端 useAuth() 返回 stub，便于看界面，不授予后端权限。
# 无真实凭据的受保护 API 仍拒绝；有真实 PAT/会话时，读写权限由后端决定。
# 不能据此保证“读成功”或“写一定 401”。
VITE_DEV_FAKE_AUTH=

# 可选：逗号分隔的 Vite allowedHosts 配置。未设置时为 localhost/127.0.0.1。
# 仅在明确需要受控反向代理预览时设置；Host 白名单不是认证。
# 不要公开带 token 的 Vite 服务；默认使用 npm run dev -- --host 127.0.0.1。
VITE_DEV_ALLOWED_HOSTS=

# 可选：仅用于隔离开发后端的最小权限 Bearer。
# 当前代理仅向 /api 注入 Authorization: Bearer，不注入 /_、/share、/swagger。
# 若使用 system PAT，它来自后端 YAML 的 system_pat.token / scopes；
# 服务端不再接受旧 QATLAS_SYSTEM_PAT 环境配置。
# 真实 Bearer 可能授权写入，也不自动满足所有用户会话/管理员路由要求。
# 不要把生产 token 填在这里；该变量仍有 VITE_ 暴露风险（见文件顶部）。
VITE_DEV_API_PAT=
