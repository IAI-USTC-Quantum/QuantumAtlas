# QuantumAtlas — qatlasd 服务端 .env 模板
#
# **只给 server（Go `qatlasd` 二进制）用**。client（Python `qatlas` CLI）
# 自 v0.16.0 起改用 YAML 配置，路径 `~/.config/qatlas/config.yaml`，
# 用 `qatlas config init/set` 管理。如果你只是装 client 跑 `qatlas upload`
# / `qatlas mineru` 等命令，**不需要这个文件**。
#
# 复制本文件为 .env 放到 server 主机上（推荐 `/etc/quantum-atlas/.env`
# 或 `~/QuantumAtlas/.env`），然后只取消注释你明确想覆盖的变量。
# 保持注释时，Go server 的 internal/config/config.go 会使用代码内置的
# 默认值（XDG 路径等，见各字段下方注释）。
#
# 也可以直接在 systemd unit 里 `Environment=KEY=VAL` 单条注入，或用
# docker `-e` / k8s `envFrom configMapRef` —— 见 docs/deployment/server-config.md。
#
# 不要提交真实 .env。token、API key、公网 URL 都应视为部署环境专属配置或密钥。
#
# ─── 命名规范 ─────────────────────────────────────────────────────────────
# 项目自有变量统一加 `QATLAS_` 前缀，避免与系统 / 其他工具变量冲突。
# 旧名（无前缀）仍作为 alias 保留，新代码请用 QATLAS_*。
#
# 第三方 / SDK 标准名不加前缀（业界生态已认这些名字，强行包就破坏复用）：
#   - NEO4J_URI / NEO4J_USER / NEO4J_PASSWORD       — Neo4j 官方驱动认这些
#   - GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET       — GitHub OAuth 标准名
# ──────────────────────────────────────────────────────────────────────────


# ============================================================================
# 公开 URL
# ============================================================================

# server 自报的对外 URL。生成 OAuth callback、share link 等回链时使用。
# 留空时 OAuth callback 退到 `Request.Host`，dev 模式可不写；生产**强烈建议**
# 显式配，免得 PocketBase 重定向到反代后看到的 host header（可能是内网名）。
# alias: PUBLIC_BASE_URL   ⚠️ DEPRECATED — removed in v0.19.0；请用 QATLAS_SERVER_URL
# QATLAS_SERVER_URL=https://atlas.example.com


# ============================================================================
# PocketBase / OAuth (server 内嵌 PocketBase, Go 二进制启动时自动注入)
# ============================================================================

# GitHub OAuth App credentials. 服务端 internal/auth/oauth.go 在 OnBootstrap
# 时把它们同步到 users collection 的 OAuth2 providers。在
# https://github.com/settings/developers (或 organization/settings/applications)
# 创建 OAuth App，callback 填 https://<your-server>/api/oauth2-redirect。
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# GitHub 用户名白名单（逗号分隔，大小写不敏感）—— 谁能完成 OAuth 登录。
# 由 internal/auth/oauth.go 的 OnRecordAuthWithOAuth2Request 钩子强制校验：
# 不在名单的 GitHub 账号拿 403、不建 users 记录、不发 session token。
# **Fail-closed**：此项与 QATLAS_ADMIN_GITHUB_LOGINS 同时为空时，谁都登不了
# （含你自己）——刻意的"默认上锁"。误锁了用 PocketBase superuser（邮箱密码
# 登 /_/）改 env 后重启即可，superuser 不受此白名单影响。
# QATLAS_ALLOWED_GITHUB_LOGINS=alice,bob

# GitHub 用户名 admin 名单（逗号分隔）。"首次 OAuth 自动提升 admin"这层
# **当前 handler 未实现**；但名单里的人会被 IsGitHubLoginAllowed 隐式视为
# 允许登录（等价于并进上面的 ALLOWED 白名单）。
# QATLAS_ADMIN_GITHUB_LOGINS=alice,bob

# 强制 server 用 tcp4-only listener（默认 off）。**只在 host 被 v4-only
# portproxy 包裹时打开**（典型场景：WSL2 + Windows netsh portproxy）。
# 普通 Linux 云 VPS 不要打开 —— 默认 PocketBase 用 dual-stack v6 socket
# 同时服务 v4 + v6 client；打开此项会关掉 v6 入口。
# 取值：1 / true / yes / on / y / t = 启用。
# QATLAS_FORCE_TCP4=0


# ============================================================================
# System PAT — 服务端运维专用 breaking-glass bearer token (server only)
# ============================================================================

# 单个、可选、与 PocketBase 完全无关的 bearer token，从环境变量加载，永远
# 不落 pb_data。专门给"没有用户登录 / pb_data 不可用 / 灾难恢复"等运维
# 场景兜底：新 edge bootstrap、CI / cron 脚本不绑定具体人、PocketBase 数据
# 损坏后的紧急访问等。
#
# 使用方式：HTTP 请求带 `Authorization: Bearer <QATLAS_SYSTEM_PAT 的值>`。
# 任何普通 user PAT (`qat_...`) 没法用的接口它都能用。
#
# **未设置 / 留空 = 功能完全关闭**（默认状态；普通用户登录 + user PAT 的
# 路径完全不受影响）。设置后启动日志会有一行 `system PAT enabled
# (length=N scopes=[...])`，方便确认 env 真被读到了 —— 只打长度不打明文。
#
# **安全说明**：能读到这个值的人 = superuser-equivalent，跟 .env 里其他
# 密钥（S3 / Neo4j / GitHub OAuth secret）同等敏感。.env 文件 mode 600 +
# 服务用户 owner 即可，跟现有约定一致。
#
# 生成办法：自己挑一个强随机串，长度至少 16 字符（服务端启动时硬校验，
# 不够长直接 fatal exit 防止有人填 placeholder 上 prod）。推荐：
#   openssl rand -base64 32
#   uuidgen
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
# 前缀格式随意 —— 不强制 `qats_` 之类的约定，你想要可读性自己挑。
#
# QATLAS_SYSTEM_PAT=

# System PAT 的 scope 列表。默认是 `*`（master / superuser，跟浏览器
# session 同权限），覆盖所有 scopeGuard 检查；最常见的 breaking-glass /
# bootstrap 场景就这一个值。
#
# 想要 least-privilege 的运维 token（例如 nightly 只读不写），改成
# CSV 显式列出。词表跟 user PAT 一样：wiki:read / papers:read /
# papers:write / graph:read / wiki:write
# （`pat scopes` 子命令可以列出）。也允许写 `*` 显式表达 master。
#
# 多个 scope 不要带空格：`wiki:read,papers:read,graph:read`。
# 留空 / 不设 = 默认 `*`。
#
# QATLAS_SYSTEM_PAT_SCOPES=*


# ============================================================================
# Server 侧
# ============================================================================

# 服务监听地址。位于本机反向代理后时使用 127.0.0.1；只有在明确配置好防火墙和
# 反向代理边界时，才考虑监听 0.0.0.0。
# alias: SERVER_HOST / SERVER_PORT   ⚠️ DEPRECATED — removed in v0.19.0；请用 QATLAS_SERVER_HOST / QATLAS_SERVER_PORT
# QATLAS_SERVER_HOST=127.0.0.1
# QATLAS_SERVER_PORT=4200

# 存储根目录。
#
# 默认值（都遵循 XDG Base Directory 规范）：
#   QATLAS_RAW_DIR     -> ${XDG_DATA_HOME:-$HOME/.local/share}/qatlasd/raw
#   QATLAS_DATA_DIR    -> ${XDG_DATA_HOME:-$HOME/.local/share}/qatlasd/data
#   QATLAS_PB_DATA_DIR -> ${XDG_DATA_HOME:-$HOME/.local/share}/qatlasd/pb_data
#
# 为什么不再默认在仓库内：
#   - 仓库内的 raw/data/pb_data 让 git 状态混乱（必须维护 .gitignore 规则）
#   - 仓库内的 raw/ 可能是 FUSE 挂载（rclone SMB），让 `go ./...` 卡死
#   - 跟 12-factor / XDG / FHS 都背道
#
# 想覆盖到其他位置（FHS / 自建挂载点 / 共享盘）时显式赋值。
#
# ⚠️ RAW_DIR 是 **dev-only fallback**：当下方 S3 / RustFS 段（QATLAS_S3_*）启用时
#    server 完全不读 RAW_DIR；生产部署强烈建议配 S3，把 LocalStore 留给 dev / CI。
#
# alias: RAW_DIR / DATA_DIR / PB_DATA_DIR   ⚠️ DEPRECATED — removed in v0.19.0；请用 QATLAS_*
# QATLAS_RAW_DIR=
# QATLAS_DATA_DIR=
# QATLAS_PB_DATA_DIR=
#
# 注：pb_data 路径**只通过 QATLAS_PB_DATA_DIR 控制**——server 启动时
# 自动转成 PocketBase 的 `--dir=` 传给底层。systemd unit 的 ExecStart
# 不需要也不应该再硬写 `--dir=...`（cmdline 会覆盖 env，给排障添麻烦）。

# 可选的服务端审计用户头，由反向代理、SSO 或 API gateway 注入。
# 留空时不记录用户头；客户端不应手动设置这个头。
# alias: USER_HEADER       ⚠️ DEPRECATED — removed in v0.19.0；请用 QATLAS_USER_HEADER
# QATLAS_USER_HEADER=X-Token-Subject

# ----------------------------------------------------------------------------
# 论文访问开关（self-hosted 可选）
# ----------------------------------------------------------------------------
# 默认 false。qatlasd 默认不对外 serve PDF / Markdown 字节；这是
# quantum-atlas.ai 等公开实例选择的合规姿态——arxiv PDF 由作者保留版权，
# 我们不替上游做二次分发，markdown 仅供 server 内部检索。
#
# Self-hosted 部署在受控范围（私有团队、内部站点）可设为 true，启用后：
#   1. 注册 GET /api/papers/{id}/markdown + /markdown/status 端点（受
#      papers:read scope 保护）
#   2. 缓存未命中时 server 用下面的 MINERU_* 配置透明触发 silent
#      conversion（独立于贡献者本地的 ~/.config/qatlas/config.yaml）
#   3. 对外分发的合规义务转由部署方承担
#
# Contributor 流程（`qatlas mineru` + POST /api/papers/{id}/upload-mineru）
# 与本开关无关，开关 false 时也照常工作。
# QATLAS_PAPER_ACCESS_ENABLED=false

# ----------------------------------------------------------------------------
# RAG 向量检索（仅当 QATLAS_PAPER_ACCESS_ENABLED=true 时生效）
# ----------------------------------------------------------------------------
# 配齐两条 URL 之后，qatlasd 注册 /api/rag/* 两条 route：
#
#   POST /api/rag/search   gated by scopeGuard("papers","read")
#   GET  /api/rag/healthz  anonymous（SPA 用这条决定是否显示"文章搜索"入口）
#
# qatlasd 自己当 Qdrant client（gRPC 直连）+ 调 embed worker（HTTP）做
# embedding/rerank。整个查询路径在 internal/routes/rag.go 实现，**没有**
# Python sidecar 这一层。
#
# 同 PAPER_ACCESS 总开关一致的合规约束：RAG 返回的 chunk_text snippet 是
# 论文 markdown 的派生片段；总开关 false 时这里设了也不生效。
#
# 部署端到端见 docs/deployment/rag.md。
#
# QATLAS_RAG_QDRANT_URL=10.144.18.10:6334        # gRPC host:port（推荐）
# QATLAS_RAG_QDRANT_URL=https://qdrant.example   # 也支持 http(s):// scheme
# QATLAS_RAG_QDRANT_API_KEY=
# QATLAS_RAG_QDRANT_COLLECTION=qatlas_papers_v1  # 默认值，跟着 ingester
# QATLAS_RAG_EMBED_URL=http://10.144.18.88:8801
# QATLAS_RAG_EMBED_TOKEN=

# ----------------------------------------------------------------------------
# Server-side MinerU（仅当 QATLAS_PAPER_ACCESS_ENABLED=true 时生效）
# ----------------------------------------------------------------------------
# 开关为 false 时这组字段被 server 忽略；为 true 但缺 MINERU_API_TOKENS 时
# `/markdown` 缓存未命中会返回 503（cache-only mode）。
# 多 token 用逗号分隔：MINERU_API_TOKENS=tok-a,tok-b,tok-c —— 一个用完了
# server 自动切下一个，全用完才返 503 + Retry-After（到次日 00:01 本地）。
# MINERU_API_TOKENS=
# MINERU_API_BASE_URL=https://mineru.net
# MINERU_MODEL_VERSION=vlm
# MINERU_LANGUAGE=ch
# MINERU_IS_OCR=false
# MINERU_ENABLE_FORMULA=true
# MINERU_ENABLE_TABLE=true
# MINERU_POLL_INTERVAL=3.0
# MINERU_TIMEOUT=1800
# MINERU_MAX_CONCURRENT_JOBS=4

# ----------------------------------------------------------------------------
# Silent fetch from arxiv.org（仅当 QATLAS_PAPER_ACCESS_ENABLED=true 时生效）
# ----------------------------------------------------------------------------
# 配置后 server 端的 GET /api/papers/{id}/markdown 和 /api/papers/{id}/pdf
# 在缓存未命中时会自动从 arxiv.org 拉对应 PDF（"静默 fetch"），写入对象
# 存储后再触发后续步骤（markdown 接着触发 MinerU 转换；pdf 直接 serve）。
# 整个流程是**异步**的：第一次调用立即返回 202 + Operation-Location，
# 客户端 poll `/markdown/status` 或 `/pdf/status` 查看进度
# （含 pdf_ready / md_ready / phase / fetch.bytes_received / convert.stage 等
# 字段）。多并发同 id 请求被 server-side dedupe 成单次 fetch。
#
# arxiv 的 bulk-data etiquette 要求请求 User-Agent 带 mailto 联系方式，
# 且单 IP rate ≤ 1 req/3s。下面的 RPS 默认 0.33（≈ 每 3 秒一次）+ burst 2
# 严格遵守。多 edge 共享同一公网 NAT 时，每台 edge 都应**调低** RPS
# 让聚合速率仍满足 ≤ 1/3s。
#
# OPENALEX_MAILTO 同时被 DOI 解析（OpenAlex polite-pool）和 arxiv fetch
# User-Agent 共用——两边都要求带联系邮箱。
#
# QATLAS_OPENALEX_MAILTO=ops@your-domain.tld
# QATLAS_ARXIV_FETCH_CONCURRENT=2       # 并行 fetch 上限（与 MinerU 独立）
# QATLAS_ARXIV_FETCH_RPS=0.33           # token-bucket 速率（req/s, 支持小数）


# ============================================================================
# 第三方 / SDK 标准名（无 QATLAS_ 前缀）
# ============================================================================

# Neo4j 连接配置。留空时 catalog 功能 disabled，相关 API endpoint 降级。
# alias: NEO4J_USER 是 NEO4J_USERNAME 的别名（Neo4j 官方两个都认）。
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=
# NEO4J_DATABASE=neo4j     # 可选，多 DB 部署用


# ============================================================================
# 对象存储（RustFS / S3 兼容）
# ============================================================================
# Go server 通过 internal/objstore 抽象层接 S3 兼容后端（minio-go SDK）。
# v0.7.0 起对象存储拆成**三个独立 bucket**（按 asset kind 分桶），外加一个
# 预留给 OpenAlex snapshot 的桶。连接字段（endpoint + 双 key）+ 三个 asset
# bucket 必须**同时**填或同时不填：
#   - 全部留空 → 走本地 RawDir（LocalStore，单目录 fallback），适合 dev 起步；
#   - 全部填上 → 走 S3 后端（objstore.Router 把 pdf/md/images 路由到各自桶），
#                server 端不再读写本地 RAW_DIR；
#   - 半填 / 缺一不可 → 启动直接报错退出（避免 reader / writer 跑两套后端
#                       的悄悄数据腐败）。
#
# ⚠️ 旧的单桶变量 QATLAS_S3_BUCKET=qatlas-raw 在 v0.7.0 已**废弃**：还残留在
#    .env 里会让 server 启动 fail-loud（提示迁到三桶）。请删掉它。
#
# 凭据生成：bash scripts/rustfs_bootstrap.sh （需要 RustFS root 凭据）
# 详细部署 / rotate / Caddy 入口模板见 docs/deployment/rustfs.md
# （概念背景见 docs/concepts/storage-architecture.md）
#
# Endpoint 必须含 scheme（https:// 或 http://），minio-go 据此决定是否
# 走 TLS。Bucket 名是单独参数，不要把 bucket 拼进 endpoint。
#
# QATLAS_S3_ENDPOINT=https://raw.your-domain.tld
# QATLAS_S3_BUCKET_PDF=qatlas-pdf
# QATLAS_S3_BUCKET_MD=qatlas-md
# QATLAS_S3_BUCKET_IMAGES=qatlas-images
# QATLAS_S3_ACCESS_KEY_ID=<bootstrap 脚本输出>
# QATLAS_S3_SECRET_ACCESS_KEY=<bootstrap 脚本输出>
#
# OpenAlex snapshot 桶（可选；仅 `qatlasd openalex bootstrap` 用）——
# 存放从 OpenAlex S3 抓回的 filtered works jsonl.gz。不参与 paper 上传写路径，
# 不属于"三桶 all-or-nothing"校验，单独留空即可（不跑 bootstrap 时无需配）。
# QATLAS_S3_BUCKET_OPENALEX_SNAPSHOT=qatlas-openalex
#
#
# QATLAS_S3_PUBLIC_ENDPOINT（可选） —— 仅用于给最终用户 client 签 presigned
# URL 的"公网入口"。server↔RustFS 走 ENDPOINT（mesh / 内网，省一跳反代），
# 但 presigned URL 里的 host 必须是公网可达的，否则 client 拿到的链接连不通。
# 留空（或与 ENDPOINT 相同）= 单 endpoint 模式，presign 用 ENDPOINT 签
# —— 仅适合开发或全内网部署，不要在生产边缘节点这么干。
#
# 部署要点：公网 host 必须反代到内网 RustFS 端口，且要 preserve Host header
# —— SigV4 验签把 Host 算进 canonical request，反代若改写 Host 会让
# RustFS 报 SignatureDoesNotMatch。最小 Caddy 模板：
#   raw.your-domain.tld {
#       reverse_proxy <internal-rustfs-host>:9000 { header_up Host {host} }
#   }
#
# QATLAS_S3_PUBLIC_ENDPOINT=https://raw.your-domain.tld


# ───────────────────────────────────────────────────────────────────────
# 边缘标识 + RustFS 写入留痕（server only；T10）
# ───────────────────────────────────────────────────────────────────────
#
# QATLAS_EDGE_NAME（可选） —— 这台 edge 的人类可读名字（如 us-east / cn-shanghai）。
# 唯一作用：折进 S3 client 的 User-Agent（qatlasd/<version>/<edge>），
# 让 RustFS notify 事件流里"正规 server 写"与"直连 mc/boto3"一眼可分。**UA 可
# 伪造，绝不用于鉴权**——取证判定主键永远是 SigV4 accessKey。多 edge 共享
# 同一把 svcacct key 时光看 accessKey 分不出哪台 edge，这个标才有意义。留空则
# UA 只到 qatlasd/<version>。
# QATLAS_EDGE_NAME=us-east
#
# 这是 qatlasd 端唯一与写入留痕相关的 env。sink 本身不在我们的 binary /
# .env 里：由一个通用、零后端约定的日志转发器（Fluent Bit）作为 sidecar 跑在
# NAS 上 RustFS 旁边，接 RustFS notify webhook（per-bucket subscribe，5 个资产桶
# PUT/DELETE 推到 sink），批量写进 qatlas-s3-events 桶。sink 用的 svcacct key
# （qatlas-s3-events-writer）、桶名、订阅列表全在 NAS 侧 Fluent Bit / RustFS
# compose 配置里（DSM GUI 管理），与 server 解耦——dumb 存储层不被后端演进的约
# 定绑死。整套部署见 docs/deployment/rustfs.md#写入留痕-audit-sink-t10。
