# mkdocs deps —— RTD 通过 .readthedocs.yaml 装这份。
# 本地预览：pip install -r docs/requirements.txt && mkdocs serve
#
# 仅文档工具；Go 开发/运行不需要 Python，主仓没有 Python 包或聚合环境。
mkdocs-material>=9.6
mkdocs-awesome-pages-plugin>=2.9

# 图片 lightbox（点开看大图）
mkdocs-glightbox>=0.4

# 页面底部显示最后更新时间（基于 git 历史）
mkdocs-git-revision-date-localized-plugin>=1.3

# 把 swaggo 生成的 OpenAPI spec 嵌成 Swagger UI（api-explorer.md）。
# 依赖全部 bundle，不走 CDN；与 Material 暗色模式联动。
mkdocs-swagger-ui-tag>=0.6
